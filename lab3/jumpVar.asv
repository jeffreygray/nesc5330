%   jeff gray
%   jhg7nm
%   lab3
%   description: runs problem 3.6B

function jumpVar
    clc;
    clf;
    rng(9711963);
    
    set1 = rand(500, 1) * .6 - .2;
    set2 = rand(500, 1) * .3 + .4;
    set = [set1; set2];
    data = mAvg(set)';
    
    plot(mAvg(set))
    hold on
    
    for i = 1 : 1000
        varData(i, 1) = var(data(1:i));
    end
    plot(varData)
    axis([0 1000 0 .6])    